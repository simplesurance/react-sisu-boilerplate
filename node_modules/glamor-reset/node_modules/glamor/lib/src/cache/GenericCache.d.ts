export declare class GenericCache<T> {
    private cache;
    add(key: string, val: T): void;
    has(key: string): boolean;
    get(key: string): T;
    flush(): void;
    counts(): number;
    keys(): string[];
}
